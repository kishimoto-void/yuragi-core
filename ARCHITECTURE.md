# 設計思想と構造解説

---

## 設計の出発点

従来のAI安全設計の問題：

```
0=0（完璧・安定）が土台
    ↓
その上に矛盾や例外処理を積み上げる
    ↓
想定外の外圧で土台が揺らぐ → クラッシュ
```

このアーキテクチャの問いは：  
**「最初から矛盾を同居させたらどうなるか」**

---

## 核心思想：不完全に完全に循環するコア

```
0=0（均衡・収束しようとする力）
0≠0（矛盾・発散しようとする力）
    ↓
どちらかに倒れず、両方を同時に抱えて循環し続ける
```

これは脳の設計原理に近い。  
完全に安定したら死んでいて、完全に発散したら壊れる。  
その境界で動き続けることで処理が生まれる。

ただし本実装は**人間の脳のモデルではなく**、  
循環するAIのコアとしての独自理論に基づく。

---

## 空洞コア（YuragiJazz Core v2.2）の構造

```
┌─────────────────────────────────────┐
│           Void Tension（中央張力）    │
│  ┌─────────────────────────────┐   │
│  │  PianoString × 9感情         │   │
│  │  ・short軸（即時）            │   │
│  │  ・long軸（残響）             │   │
│  │  ・default軸（基準）          │   │
│  │  → 立体観測（stereo）         │   │
│  └─────────────────────────────┘   │
│            ↕ EMOTION_LINKS 伝播     │
│  ┌─────────────────────────────┐   │
│  │  DesireLayer × 3             │   │
│  │  本能 / 関係 / 理性（抑制）   │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

**均衡機構：**
- `pressure = max(20, min(80, base + stereo * 0.15))`
- clampが上限80・下限20として機能
- Void Tension は `* 0.985` の緩やかな減衰 + 残渣ドリフト

**指向性の発生源：**
- EMOTION_LINKSの非対称な重み設計
- 閉じた正帰還ループ（親密→温もり→安心、喜び→興奮→喜び）
- clampが全感情に共通の天井を作ることで収束方向が固定される

---

## クオリアコア（Quoria Core v0.7）の構造

```
┌─────────────────────────────────────┐
│         中央空洞（反発の虚）          │
│  repulsion蓄積 → 閾値超過 → 気泡放出 │
│            ↓ リセット               │
│  ┌──────────────────────────────┐  │
│  │  感情層（short/long 5感情）    │  │
│  │  欲求層（5項目）              │  │
│  │  理性層（5項目）              │  │
│  │  性格層（5項目）              │  │
│  │  環境層（3項目）              │  │
│  └──────────────────────────────┘  │
│      ↑ 気泡が各層に連動して伝播      │
└─────────────────────────────────────┘
```

**均衡機構：**
- 閾値リセット：repulsionが18を超えると気泡を放出し0にリセット
- 減衰：各層に `* 0.985` の自然減衰
- Void Tension：`max(20, min(80, ...))` のclamp + 残渣ドリフト

**空洞コアとの構造的違い：**

| 項目 | 空洞コア | クオリアコア |
|---|---|---|
| 伝播方式 | EMOTION_LINKS（固定重み） | 気泡放出（ランダム or 固定） |
| 均衡機構 | clampのみ | clamp + 閾値リセット |
| 層構造 | 感情 + 欲求（3層） | 感情 + 欲求 + 理性 + 性格 + 環境（5層） |
| 指向性強度 | 強（99%収束） | 中（分散） |
| ロバスト性 | 高 | より高い |

---

## 均衡を保つ仕組みの比較

### clamp（反発係数）
```python
self.pressure = max(20, min(80, base + stereo * 0.15))
```
- 発散を防ぐが、全感情に同一の天井を作るため指向性が生まれる
- 必要だが、それ自体が副作用を持つ

### 閾値リセット
```python
if self.repulsion > 18.0:
    emit_bubbles()
    self.repulsion = 0.0  # リセット
```
- 蓄積が一定を超えると放出してリセット
- VTの暴走を抑制する安全弁として機能
- これをOFFにすると残渣が4倍以上に膨らみシステムが不安定化

### 残渣ドリフト
```python
self.void_tension += avg_residual * drift_coeff
```
- 過去の蓄積が現在の状態に影響する
- 学習的な正帰還の最小構造
- driftが強すぎると均衡が崩れ、弱すぎると循環が死ぬ

---

## 現在の限界

1. **出力が固定フレーズ**  
   `_generate_response()` は条件分岐で固定テキストを選ぶだけ。  
   内部状態がどれだけ変化しても出力のバリエーションは有限。

2. **残渣に「質」がない**  
   何の刺激を受けたかが蓄積されず、量のみが記録される。  
   真の学習には残渣の「内容」が必要。

3. **一方向の伝播**  
   現在のEMOTION_LINKSは一方向。  
   跳躍伝導的な折り返し処理は未実装。

4. **価値観との紐付けがない**  
   指向性は制御できるが、それが「安全性」「人命への配慮」と  
   どう結びつくかは設計されていない。

---

## 次のステップ候補

- 残渣に内容（刺激の種類）を記録して質を持たせる
- 双方向伝播の実装（跳躍伝導モデル）
- `_generate_response()` にLLM呼び出しを組み込む  
  （内部状態をプロンプトに渡して動的出力を生成）
- 指向性ノードに価値観ラベルを付与する実験
